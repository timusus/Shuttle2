{
  "commits": [
    "Disabled code minification (fixed crash when launching)",
    "Enabled 'legacy storage', potentially fixing playback issues on some devices",
    "Replaced Crashlytics with Bugsnag"
  ]
}